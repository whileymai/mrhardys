<!-- /templates/page.liquid -->
<section class="section">
  <div class="container container-ver2">
    <div class="custom-content engoc_60">
      <h1>{{ page.title }}</h1>

        {% comment %}
        Regular page content goes here.
        {% endcomment %}
        <div class="rte">
          {{ page.content }}
        </div>
      </div>
    </div>
</section>
