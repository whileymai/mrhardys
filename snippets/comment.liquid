<!-- /snippets/comment.liquid -->
{% comment %}

This snippet generates the structure of a comment.
It is used to show not only recent comments, but also comments that were
just published but not yet approved.

More information:
- http://docs.shopify.com/themes/liquid-variables/comment

{% endcomment %}

<li class="media">
  <a class="pull-left" href="#comment_form">
    <img class="media-object img-circle" src="https://cdn.shopify.com/s/files/1/0588/1313/files/review-logo.jpg?4255835597079716684" alt="images"> 
  </a>
  <div class="media-body">
    <div class="text">
      <h3>{{ comment.author }}</h3>
      <p class="date">{{ comment.created_at | date: '%B %d, %Y' }} at {{ comment.created_at | date: "%R" }}</p>
      <p>{{ comment.content }}</p>
      <!-- Nested media object -->
      <a class="reply" href="#comment_form" title="{{ 'blogs.comments.reply' | t}}"><i class="icons icon-action-undo"></i> {{ 'blogs.comments.reply' | t}}</a>
    </div>
    <!-- End text -->
  </div>
</li>